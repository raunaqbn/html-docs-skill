interface:
  display_name: "HTML Docs"
  short_description: "Publish HTML and embed locally rendered videos"
  default_prompt: "Use $html-docs to publish this document and, when motion helps, author and locally render a deterministic embedded video."

policy:
  allow_implicit_invocation: true
