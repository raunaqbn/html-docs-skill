interface:
  display_name: "HTML Docs"
  short_description: "Publish HTML and build cue-synced explainer videos"
  default_prompt: "Use $html-docs to publish this document and, when motion helps, author, audit, and locally render a modular cue-synced explainer video."

policy:
  allow_implicit_invocation: true
